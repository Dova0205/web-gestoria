---
---
<section class="hero-section">
  <div class="overlay"></div> <div class="content">
    <h1>Protegemos lo que más te importa</h1>
    <p>Asesoramiento integral en seguros con la confianza y experiencia de <strong>Silbert Seguros</strong>.</p>
    
    <div class="cta-buttons">
      <a href="/#servicios" class="btn-primary">Cotizá tu Seguro</a> <a href="/contacto" class="btn-secondary">Hablemos</a> 
    </div>
  </div>
</section>

<style>
  .hero-section {
    position: relative;
    background-image: url('/portada.jpg');
    background-size: cover;
    background-position: center;
    height: 85vh; /* Ocupa el 85% de la altura de la pantalla, muy impactante */
    display: flex;
    align-items: center; /* Centra verticalmente */
    justify-content: center; /* Centra horizontalmente */
    text-align: center;
    color: white;
  }

  /* El filtro azulado estilo National Brokers */
  .overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 51, 102, 0.7); /* Azul corporativo #003366 con transparencia */
    z-index: 1;
  }

  .content {
    position: relative;
    z-index: 2; /* Para que el texto quede ENCIMA del filtro azul */
    max-width: 800px;
    padding: 0 20px;
  }

  h1 {
    font-size: 3.5rem;
    margin-bottom: 1rem;
    font-weight: 800; /* Letra bien gruesa */
    line-height: 1.2;
  }

  p {
    font-size: 1.25rem;
    margin-bottom: 2rem;
    color: #f0f0f0;
  }

  .cta-buttons {
    display: flex;
    gap: 20px;
    justify-content: center;
  }

  /* Botón Principal (Naranja/Rojo) */
  .btn-primary {
    background-color: #ff4500;
    color: white;
    padding: 15px 30px;
    text-decoration: none;
    border-radius: 5px;
    font-weight: bold;
    text-transform: uppercase;
    transition: transform 0.2s;
  }

  .btn-primary:hover {
    background-color: #e03e00;
    transform: scale(1.05); /* Efecto de "crecer" un poquito */
  }

  /* Botón Secundario (Borde Blanco transparente) */
  .btn-secondary {
    border: 2px solid white;
    color: white;
    padding: 15px 30px;
    text-decoration: none;
    border-radius: 5px;
    font-weight: bold;
    text-transform: uppercase;
    transition: background 0.3s, color 0.3s;
  }

  .btn-secondary:hover {
    background: white;
    color: #003366;
  }

  /* Ajuste para celulares */
  @media (max-width: 768px) {
    h1 { font-size: 2.5rem; }
    .cta-buttons { flex-direction: column; }
  }
</style>