---
---
<section class="hero-section">
  <div class="overlay"></div> 
  <div class="content">
    <h1>30 Años de Experiencia, Respaldo y Confianza.</h1>
    <p>Protegemos lo que más te importa para que vivas tranquilo.</p>
    
    <div class="cta-buttons">
      <a href="/#servicios" class="btn-primary">Cotizá tu Seguro</a> 
      <a href="/contacto" class="btn-secondary">Hablemos</a> 
    </div>
  </div>
</section>

<style>
  .hero-section {
    position: relative;
    background-image: url('/portada.jpg');
    background-size: cover;
    background-position: center;
    height: 80vh;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    color: white;
  }

  /* Blue Overlay */
  .overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 51, 102, 0.7);
    z-index: 1;
  }

  .content {
    position: relative;
    z-index: 2;
    max-width: 800px;
    padding: 0 20px;
  }

  h1 {
    font-size: 3.5rem;
    margin-bottom: 1rem;
    font-weight: 800;
    line-height: 1.2;
  }

  p {
    font-size: 1.25rem;
    margin-bottom: 2rem;
    color: #f0f0f0;
  }

  .cta-buttons {
    display: flex;
    gap: 20px;
    justify-content: center;
  }

  /* Buttons */
  .btn-primary {
    background-color: #ff4500;
    color: white;
    padding: 15px 30px;
    text-decoration: none;
    border-radius: 5px;
    font-weight: bold;
    text-transform: uppercase;
    transition: transform 0.2s;
  }

  .btn-primary:hover {
    background-color: #e03e00;
    transform: scale(1.05);
  }

  .btn-secondary {
    border: 2px solid white;
    color: white;
    padding: 15px 30px;
    text-decoration: none;
    border-radius: 5px;
    font-weight: bold;
    text-transform: uppercase;
    transition: background 0.3s, color 0.3s;
  }

  .btn-secondary:hover {
    background: white;
    color: #003366;
  }

  /* Mobile */
  @media (max-width: 768px) {
    h1 { font-size: 2.5rem; }
    .cta-buttons { flex-direction: column; }
  }
</style>